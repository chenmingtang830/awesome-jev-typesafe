Thanks for the pull request. Two lines of context help the review:

What this adds or changes:

Which rule in contributing.md it meets (five lists, twenty stars, fills a gap, or publishes measured numbers):

Before you open it, run `npx awesome-lint`, `npm test`, and `npm run parse`, and commit the regenerated `data/projects.json` if it changed.
